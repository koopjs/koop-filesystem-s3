
language: node_js

node_js:
  - 8
  - 10
sudo: false # Enable docker-based containers
cache:
  directories: # Cache dependencies
    - node_modules
env:
  global:
    - NODE_ENV=test
script:
  - npm test
